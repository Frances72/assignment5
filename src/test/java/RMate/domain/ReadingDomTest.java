package RMate.domain;

/**
 * Created by User on 2015/09/13.
 */
public class ReadingDomTest {
    public void testCreate() throws Exception{

/*
        Reader reader = new Reader.Builder(9.99).build();
        Cycler cycler = new Cycler.Builder("Mitzi").last("Goldberg").age(63).build();
        Chip chip = ChipFactory.createChip(cycler);

        Reading reading = ReadingFactory.createReading(chip.getChipID(), cycler.getCyclerID(),
                reader.getReaderId(),reading.getReadTime()); */
    }

}
